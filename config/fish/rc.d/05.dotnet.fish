# Dotnet CLI completions for Fish

# Enable dotnet completions if available
# dotnet complete --shell fish | source  # This might work, but let's check

# For now, commented as in original
# dotnet complete --shell fish | source
